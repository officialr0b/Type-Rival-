name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm peers check
      - run: pnpm typecheck
      - run: pnpm typecheck:realtime
      - run: pnpm lint
      - run: pnpm test
      - run: pnpm test:realtime
      - run: pnpm build
      - name: Install browser test engines
        run: pnpm exec playwright install --with-deps chromium msedge firefox webkit
      - name: Test typing in Chrome, Firefox, and Safari engines
        run: pnpm test:e2e
      - name: Test typing in Microsoft Edge
        run: pnpm test:e2e:edge
      - name: Audit production dependencies
        run: |
          pnpm audit --prod --audit-level high || {
            echo "The npm advisory service failed or reported a finding; retrying once in 15 seconds."
            sleep 15
            pnpm audit --prod --audit-level high
          }
